---
# Exception: the spec snapshots under `2_raw/specs/` use their own two-line HTML-comment header instead of this frontmatter — see that folder's README.
type: raw                 # raw | thought | experiment-log
topic: {topic}
title: {Title}
source: {URL or origin description}
collected: {YYYY-MM-DD}
published: {YYYY-MM-DD or Unknown}
fabric_release: {YYYY-MM or Unknown}   # the release the source describes, if known
---

# {Title}

{Original content below. Preserve the source text faithfully. Clean formatting noise
(extra whitespace, broken HTML, navigation chrome), but do not rewrite opinions or
alter meaning. This file is immutable once written — the wiki cites it as provenance.}
